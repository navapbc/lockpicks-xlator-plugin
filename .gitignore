.DS_Store
__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
.venv/

.claude/settings.local.json

# Save docs to the private repo.
# docs and domains are symlinks to the private repo's docs directory
docs
domains

# Local files for compound-engineering plugin that shouldn't be committed.
compound-engineering.local.md

# Catala/clerk build artifacts (created by clerk start, clerk build, clerk typecheck)
_build/
_targets/
