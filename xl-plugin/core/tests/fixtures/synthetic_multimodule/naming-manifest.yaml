# Synthetic two-module naming manifest (U9 Step 1.5 fixture).
# Carries the type-extended schema per U7 so test-emission consumers
# can derive scope-input types without re-parsing the Catala source.
# v3.0 schema: provenance lives in `observations:` lists (none of these
# entries carry observations — synthetic fixtures without source attribution).
version: "3.0"
inputs:
  Household:
    gross_earned_income:
      type: money
    household_size:
      type: integer
    excluded_amount:
      type: money
    remaining_earned_income:
      type: money

computed:
  income_limit:
    type: money
  income_eligible:
    type: boolean

outputs:
  is_eligible:
    type: boolean
