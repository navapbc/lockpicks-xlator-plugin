version: "3.0"
computed:
  field_a:
    observations:
      - policy_phrase: "field a — declared only in ModuleA.catala_en"
  field_b:
    observations:
      - policy_phrase: "field b — declared only in ModuleB.catala_en"
